name: Build and Push to Dev

on:
  push:
    paths:
      - '/Dockerfile' 
      - '.github/workflows/build-and-push-dev.yml' 
    branches:
      - main
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag for the Docker image'
        required: false
        default: ''

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      DEV_IMAGE_NAME: stellarhub/dev-target

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Docker Build Tag
      id: vars
      run: |
        TIMESTAMP=$(date +'%Y%m%d%H%M%S')
        TAG="${{ github.event.inputs.tag }}"
        if [ -z "$TAG" ]; then
          TAG=$TIMESTAMP
        fi
        echo "TAG=$TAG" >> $GITHUB_ENV

    - name: Display Tag for Confirmation
      if: ${{ github.event_name == 'workflow_dispatch' }}
      run: |
        echo "Using tag: $TAG"

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_ACCESS_TOKEN }}

    - name: Build Docker image
      run: docker build -t ${{ env.DEV_IMAGE_NAME }}:${{ env.TAG }} .

    - name: Push Docker image with provided or default tag
      run: docker push ${{ env.DEV_IMAGE_NAME }}:${{ env.TAG }}

    - name: Get image digest
      id: get_digest
      run: |
        DIGEST=$(docker inspect --format='{{index .RepoDigests 0}}' ${{ env.DEV_IMAGE_NAME }}:${{ env.TAG }} | cut -d'@' -f2)
        echo "DIGEST=$DIGEST" >> $GITHUB_ENV

    - name: Push Docker image with latest tag
      run: |
        docker tag ${{ env.DEV_IMAGE_NAME }}:${{ env.TAG }} ${{ env.DEV_IMAGE_NAME }}:latest
        docker push ${{ env.DEV_IMAGE_NAME }}:latest

    - name: Save Dockerfile and tag information to JSON
      run: |
        echo "{\"image_name\": \"${{ env.DEV_IMAGE_NAME }}\", \"tag\": \"${{ env.TAG }}\", \"digest\": \"${{ env.DIGEST }}\"}" > dev-build-info.json

    - name: Upload build info as artifact
      uses: actions/upload-artifact@v4
      with:
        name: dev-build-info
        path: dev-build-info.json
